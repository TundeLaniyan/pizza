@page "/myorders/{_id}"
@using Data
@using Model
@inject ShoppingBasketService PizzaOrder
@inject OrderService OrderService

<div class="order">
    <div class="top">
        <div class="order__time">Order placed @Order.OrderDetail.time</div>
        <div class="order__status">Status: <span class="font-weight-bold">@Order.Status</span></div>
    </div>
    @*<div class="body">
        <div class="body__left">
            <OrderListComponent   PizzaList=@PizzaOrder.ShoppingList TotalPizzaPrice=@PizzaOrder.TotalPizzaPrice valid=@PizzaOrder.IsPaid />
        </div>
        <div class="body__right"></div>
    </div>*@
</div>

@code {
    string status = "Pending";

    [Parameter]
    public string _id { get; set; }

    OrderModel Order;

    protected override void OnInitialized()
    {
        base.OnInitialized();

        Order = OrderService.FindById(_id);
    }

}
